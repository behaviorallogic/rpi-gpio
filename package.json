{
  "name": "rpi-gpio",
  "version": "1.0.0",
  "description": "Simple tool to toggle GPIO pins on the Raspberry Pi",
  "main": "gpio-tool.js",
  "scripts": {
    "start": "node gpio-tool.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Broski",
  "license": "GPL-3.0-or-later",
  "optionalDependencies": {
    "pigpio": "^3.3.1"
  },
  "dependencies": {
    "websocket": "^1.0.35"
  }
}
